/**
 * The class name contract for the context-menu component
 */
export interface SelectClassNameContract {
    select: string;
    select_contentDisplay: string;
    select_toggle: string;
    select_menu: string;
    select__menuOpen: string;
    select__disabled: string;
}
